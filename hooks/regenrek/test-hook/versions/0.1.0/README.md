# regenrek.test-hook

Standalone reference hook config published directly to the AgentRig registry.
Mirrors the bundled `hooks/hooks.json` from `regenrek.test-submission`,
packaged as its own signed artifact to verify standalone-hook ingestion.

Source: https://github.com/regenrek/agentrig-test-plugin (v0.2.0,
`hooks/hooks.json`).
